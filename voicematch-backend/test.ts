// import fs from 'fs';

console.log('Hello');
// console.log('Hello, World2!');

// const data = fs.readFileSync('.contentlayer/generated/Blog/blog__langchain-on-step-functions.mdx.json', 'utf-8');

// const jsonData = JSON.parse(data);

// const body_code = jsonData['body']['code'];



// const extractImages = async (mdxCode: string) => {
//   const images: { title: string, path: string }[] = []

//   const mdxAST = await parse(mdxCode)
//   unified()
//     .use(remarkMdx)
//     .use(() => (tree) => {
//       visit(tree, 'image', (node: any) => {
//         images.push({ title: node.alt, path: node.url })
//       })
//     })
//     .runSync(mdxAST)

//   return images
// }



// const images = await extractImages(mdxCode)
